@model Domain.Entities.Login
@{
    ViewBag.Title = "Login";
}

@using (Html.BeginForm("Login", "Login", FormMethod.Post))
{
    <fieldset>
        <legend>Logowanie</legend>

        @Html.AntiForgeryToken()
        @Html.ValidationSummary(true)
        @if (@ViewBag.Message != null)
        {
            <div style="border: 1px solid red">
                @ViewBag.Message
            </div>
        }
        <table>
            <tr>
                <td>@Html.LabelFor(a => a.Imie)</td>
                <td>@Html.TextBoxFor(a => a.Imie)</td>
                <td>@Html.ValidationMessageFor(a => a.Imie)</td>
            </tr>
            <tr>
                <td>
                    @Html.LabelFor(a => a.Pass)
                </td>
                <td>
                    @Html.PasswordFor(a => a.Pass)
                </td>
                <td>
                    @Html.ValidationMessageFor(a => a.Pass)
                </td>
            </tr>
            <tr>
                <td></td>
                <td>
                    <input type="submit" value="Login" />
                </td>
                <td></td>
            </tr>
        </table>
    </fieldset>
}